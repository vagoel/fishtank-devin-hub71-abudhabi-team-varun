interface:
  display_name: "Pointer Trail Emitter"
  short_description: "Cursor trails that respond to hand speed"
  default_prompt: "Use $pointer-trail-emitter to add a mote trail that emits by distance travelled, so spacing stays continuous at any hand speed."
